Navigation comments document required installs: `yarn add @react-navigation/native @react-navigation/native-stack @react-navigation/bottom-tabs react-native-safe-area-context react-native-screens`; `ThreatRing` additionally requires `react-native-svg` and `react-native-reanimated` to be installed via `expo install`. Fonts (Inter, Noto Nastaliq Urdu) must be loaded through `expo-font` in the app's root before use.